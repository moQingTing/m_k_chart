@Deprecated('Use package:m_k_chart/m_k_chart.dart instead.')
library flutter_k_chart;

// Compatibility entrypoint for applications using the package's old name.
// Keep this as a pure forwarding barrel so it cannot drift from the canonical
// public API again.
export 'm_k_chart.dart';
